@inherits QuestionContent

<CompetitorResponses>
    <CompetitorTemplate Context="competitor">
        @{
            var response = GetCompetitorAnswerId(competitor.Id);
        }
        <Track TrackId="@response" Color="@competitor.Color" UseDarkText="@competitor.UseDarkText" />
    </CompetitorTemplate>
</CompetitorResponses>

<MudPaper Class="d-flex flex-column justify-center align-content-center align-center" Elevation="0">
    <PieChart Title="Responses" Data="@ResponseData" SelectionCallback="OnSelectedChartDataPoint" />
</MudPaper>
